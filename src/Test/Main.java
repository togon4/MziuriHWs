package Test;
import java.util.List;
import java.util.Scanner;
import static Test.KthLargestElement.findKthLargest;
import static Test.PascalsTriangle.generate;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        // 1
//        System.out.print("Enter the size of the array: ");
//        int n = scanner.nextInt();
//        int[] nums = new int[n];
//
//        System.out.println("Enter " + n + " integers:");
//        for (int i = 0; i < n; i++) {
//            nums[i] = scanner.nextInt();
//        }
//
//        System.out.print("Enter the value of k: ");
//        int k = scanner.nextInt();
//
//        if (k <= 0 || k > n) {
//            System.out.println("Invalid value of k. It should be between 1 and " + n);
//        } else {
//            int result = findKthLargest(nums, k);
//            System.out.println("The " + k + "-th largest element is: " + result);
//        }
        // 2
//        System.out.print("Enter the number of rows: ");
//        int numOfRows = scanner.nextInt();
//        scanner.close();
//
//        List<List<Integer>> result = generate(numOfRows);
//
//        System.out.println(result);

        // Tests
        // 1 - ა) byte კოდი რომელიც წარმოიქმნება კომპილატორის მიერ და ამუშავებს JVM.
        // 2 - ბ) სერიალიზაცია
        // 3 - ბ) double df123 = 20.5;
        // 4 - ა) 2;  8
        // 5 - გ) byte, short, int, long
        // 6 - დ) I
        // 7 - გ) 35
        // 8 - ა) კომპილაციის შეცდომა
        // 9 - ბ) 18
        // 10 - ა) 3
        // 11 - დ) true true
        // 12 - დ) 10
        // 13 - გ) 100.0
        // 14 - ბ) b
        // 15 - დ) 1 3 5 7 9
        // 16 - ა) 1 5 7
        // 17 - გ)1
        // 18 - ბ) int b[] = new int[];
        // 19 - დ) ინკაფსულაციის დროს კლასის ველები არის private და საჭიროებისამბერ მისთვის ვქმნით ირიბად წვდომის საშუალებებს
        // 20 - გ) კლასებისთვის, პაკეტებისთვის და მეთოდებისთვის სწორი სახელის შერჩევას.
        // 21 - ბ)კლასის ველები წარმოადგენენ ობიექტის მახასიათებელი პარამეტრებს, კონსტრუქტორი მისი შექმნის შესაძლებლობა, ხოლო მეთოდები მისი დამახასიათებელი ფუნქციები.
        // 22 - ა) რეკურსიული მეთოდი იძახებს თავის თავს.
        // 23 - ა) მთელი რიცხვი ფაქტორიალს(1-დან n-მდე ნამრავლს)
        // 24 - ბ)
        // 25 - დ)
        // 26 - დ)
        // 27 - ა)
        // 28 - ბ)
        // 29 - ა)
        // 30 - გ)
        // 31 - დ)
        // 32 - გ)
        // 33 - ა)
        // 34 - ბ)
        // 35 - ა)
        // 36 - გ)
        // 37 - ა)
        // 38 - ა)







    }
}
